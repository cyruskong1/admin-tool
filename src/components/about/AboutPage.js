import React from "react";

const AboutPage = () => (
  <div>
    <h1>About</h1>
    <p>
      This app uses React, Redux, React Router DOM and other helpful libraries.
    </p>
  </div>
);

export default AboutPage;
